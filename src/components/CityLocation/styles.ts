import styled from 'styled-components/native';
import colors from '../../styles/colors';
import fonts from '../../styles/fonts';

export const CityLocationName = styled.Text`
  font-family: ${fonts.primary};
  font-weight: bold;
  font-size: 18px;

  color: ${colors.captions};
`;
